# HERMES_UID / HERMES_GID align ~/.hermes bind-mount ownership with this host user (1000:100).
# Host /box (NixOS fileSystems."/box") is bind-mounted at /workspace in hermes + dashboard.
name: hermes

include:
  - honcho/compose.yaml

services:
  hermes:
    image: nousresearch/hermes-agent:latest
    container_name: hermes
    restart: unless-stopped
    command: gateway run
    depends_on:
      honcho_api:
        condition: service_started
    ports:
      - "8642:8642"
    volumes:
      - ~/.hermes:/opt/data
      - /box:/box
    environment:
      HERMES_UID: "1000"
      HERMES_GID: "100"
    networks:
      - default
      - mcp-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"

  dashboard:
    image: nousresearch/hermes-agent:latest
    container_name: hermes-dashboard
    restart: unless-stopped
    command: dashboard --host 0.0.0.0 --insecure --tui
    ports:
      - "9119:9119"
    volumes:
      - ~/.hermes:/opt/data
      - /box:/box
    environment:
      HERMES_UID: "1000"
      HERMES_GID: "100"
      GATEWAY_HEALTH_URL: http://hermes:8642
    depends_on:
      - hermes
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"

networks:
  mcp-net:
    name: mcp-net
    driver: bridge

name: hermes

services:
  hermes:
    build: ./src
    container_name: hermes
    restart: unless-stopped
    command: gateway run
    volumes:
      - ./data:/opt/data
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
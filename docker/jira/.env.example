# Copy to `.env` (gitignored). API token: Atlassian account → Security → API tokens.
# Used by both MCP containers on `mcp-net` (HTTP MCP on container port 8000).
JIRA_BASE_URL=https://yourcompany.atlassian.net
JIRA_EMAIL=you@example.com
JIRA_API_TOKEN=
